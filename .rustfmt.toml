edition = "2024"
max_width = 100
hard_tabs = false
tab_spaces = 4
newline_style = "Unix"
use_field_init_shorthand = true
use_try_shorthand = true
# NOTE: imports_granularity / group_imports are nightly-only rustfmt options.
# Toolchain is pinned to stable (rust-toolchain.toml), so they are omitted to
# keep `cargo fmt` output warning-free. Re-add under a nightly fmt job if desired.
