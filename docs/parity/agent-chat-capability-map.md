# Agent-chat Capability Map

Status: P263 reconciled baseline. This is the authoritative local map for replacing
`/Users/zhangalex/Work/Projects/consult/agent-chat` with agentd.

Current conclusion: agentd cannot fully replace agent-chat yet. The required
replacement rows below have no `unknown` status, but many are still `partial`
or `missing`; those rows are the work queue for future specs.

P263 freezes P200-P262 in this worktree as the only authoritative implemented
range. Conflicting sibling-worktree P202-P228 artifacts count only through the
explicit dispositions in `agent-chat-worktree-reconciliation.md`; unported
code and migration versions are not implementation evidence here.

Roadmap status (2026-07-12):
`2026-07-09-agentd-native-runtime-roadmap.md` is the canonical execution order.
The former Phase I rows below now point to AD-E phases. P272-P275 remain paused
FSF-0 transitional parity candidates and do not form a competing implementation
sequence.

| Capability ID | Category | Priority | Agent-chat source | Agentd status | Replacement decision | Canonical phase |
| --- | --- | --- | --- | --- | --- | --- |
| real_codex_execution | real_execution | required | /Users/zhangalex/Work/Projects/consult/agent-chat/README.md | covered | p201 added role-prefixed Codex runtime selection and Codex-only preflight; p202 added Codex MCP launcher config overrides; p203 added the runtime matrix; p204-codex-matrix-r9 finished a Codex-only real `--execute` run through implement, lifecycle verification, three Codex reviewers, publish, open PR, and acceptance reporting. Evidence: `.agentd/real-execute-smoke/p204-codex-matrix-r9/summary.txt`. | Phase B |
| agent_registry_lifecycle | registry | required | /Users/zhangalex/Work/Projects/consult/agent-chat/backend-v2.js | partial | p213 adds durable local register, list, inspect, heartbeat, and offline surfaces through `/api/agents`, `ProductionRunHost`, `agent_repo`, and `agentctl agent`; p214 adds `launch-env`, Codex-testable `start` through `AgentBackend::spawn`, and minimal runtime observation updates; p234 adds operator-local `down`, `rebind`, session recovery from stored tmux targets, and runtime lifecycle metadata on agent records. This remains partial until auth/import hardening, dashboard, Matrix, remote relay, cutover, rollback, token provisioning, full agent home/profile management, and broader operational parity are covered. | Phase C |
| agent_runtime_profiles | registry | required | /Users/zhangalex/Work/Projects/consult/agent-chat/lib/agent-home-v1.js | partial | p213 stores backward-compatible runtime profile metadata on agent records; p214 exposes the runtime profile through `launch-env` and maps profile model/extraArgs into start-time launch environment. This remains partial until import/update/profile-management and auth semantics are covered. | Phase C |
| messaging_inbox | messaging | required | /Users/zhangalex/Work/Projects/consult/agent-chat/lib/mcp-server-core.js | partial | p217 adds durable direct messages, an operator `POST /api/messages` write path, and `check_inbox` preview/drain read semantics with agent-facing `messages`/`dm`/`group` fields; p218 resolves the direct send side of the send/post MCP gap by adding direct `send_message` MCP writes with an explicit sender; p219 adds stdio identity binding, closing the implicit identity gap for stdio direct messaging so identity-bound MCP sessions get an implicit direct sender, implicit own inbox reads, and spoof rejection before local or proxy `tools/call` dispatch; p220 adds group messaging through durable groups, group mentions in `check_inbox.group`, group history reads, HTTP group message endpoints, MCP `post`, MCP `check_group`, send/post MCP compatibility, and identity-bound stdio group tool injection; p221 adds durable local-file attachments metadata for direct and group messages through `send_message`, `post`, `/api/messages`, `check_inbox`, and `check_group`; p222 adds HTTP media stage/fetch and preserves `staged=true` attachments metadata in delivered messages; p223 adds stdio MCP proxy media cache localization for `check_inbox` and `check_group` so remote/isolated agents receive readable local paths; p224 adds message import and shadow audit for `messages.json`, `groups.json`, and `cursors.json`, preserving direct/group message history and cursor read state during migration; p225 adds task import and task-graph snapshot migration support for cutover state that messages can reference. This remains partial until remaining attachments/media parity, Matrix/remote relay delivery, notification gates, dashboard message views, live task CRUD and DAG dispatch, service cutover, rollback, and token provisioning are complete. | Phase D |
| group_messaging | messaging | required | /Users/zhangalex/Work/Projects/consult/agent-chat/backend-v2.js | partial | p220 adds durable groups, deduplicated group membership, durable group messages, group mentions, group history preview/read_all semantics, HTTP group administration, MCP `post`, and MCP `check_group`; p221 adds local-file attachment metadata on group messages and group history reads; p224 adds group message import, shadow audit, group creation from `groups.json`, and cursor preservation from `cursors.json`; p225 adds task import and task-graph snapshot migration support for related cutover state. This remains partial until full attachments/media staging, Matrix/remote relay group delivery, notification gates, dashboard message views, live task CRUD and DAG dispatch, service cutover, rollback, token provisioning, and broader agent-chat suppression/auth semantics are covered. | Phase D |
| attachments_media | messaging | required | /Users/zhangalex/Work/Projects/consult/agent-chat/lib/mcp-server-core.js | partial | p221 defines a local-file attachment rule agents can rely on: up to 8 readable non-empty files, each up to 20 MiB, persisted as agent-chat-style `path`, `name`, `mime`, `kind`, `size`, `staged=false`, and `source_path` metadata on direct and group messages. p222 adds local byte staging/fetch through `/api/media/stage` and `/api/media/fetch`, writes staged bytes under the daemon media directory, returns `staged=true` attachment metadata, and preserves that metadata in `/api/messages` delivery. p223 adds LocalPath localization in the stdio MCP proxy media cache: proxied `check_inbox` and `check_group` responses fetch staged media through `/api/media/fetch`, cache bytes on the agent side, rewrite attachment paths and `LocalPath:` text lines to readable local paths, reuse warm cache files, and warn without failing when fetch fails. This remains partial until Matrix media transfer, remote relay media handling, image sanitization, dashboard previews, and import/cutover are complete. | Phase D |
| task_graph_coordination | task_graph | required | /Users/zhangalex/Work/Projects/consult/agent-chat/lib/task-graph.js | partial | p225 adds non-destructive task import, task-graph snapshot preservation, and shadow audit for `tasks.json` and optional `task_graphs.json` into agentd compatibility tables. p226 adds live task CRUD by making the imported task rows live through agent-chat-compatible `/api/tasks` CRUD, comments, execution metadata, assignee task listing, and lifecycle transitions. p227 adds live `/api/task-graphs` CRUD, DAG dispatch direct messages, assignee node updates, result-message handling, dependency advance, condition skip, cancel, and daemon persistence. p228 adds the durable pool scheduler baseline with `/api/pool`, `/api/dispatch`, reservations, queue, provision plans, and release semantics. p229 adds task-graph scheduler integration: scheduled nodes can request role/capability, dispatched nodes carry reservation metadata, queued ticket drain dispatches the next graph node, and result-time release frees the scheduler reservation. p230 adds workflow scheduler allocation for `codergen` and `fan_out`, with scheduler metadata in run events and release behavior on completion. p231 adds existing-pane prompt reuse for routed online agents through tmux rebind and allocation-aware backend dispatch, so no duplicate spawn occurs for scheduler-routed workflow prompts. p232 adds queued codergen workflow wakeup: scheduler release-drain dispatch validates the parked task run, reuses the original task-run/worktree, and provides duplicate-dispatch suppression for replayed completion. p233 closes the remaining queued workflow wakeups / fan_out queued wakeups gap with queued fan_out reviewer wakeup: release-drain dispatch validates the parked review run, binds the reviewer prompt to the freed Codex review agent, and preserves duplicate-dispatch suppression. This remains partial until dashboard views, Matrix/remote relay state, service cutover, rollback, notification gates, and token provisioning are complete. | Phase D |
| pool_scheduler | scheduler | required | /Users/zhangalex/Work/Projects/consult/agent-chat/backend-v2.js | partial | p228 adds the agent-chat pool scheduler baseline: role/capability inference, `/api/pool` visibility, `/api/dispatch` route/queue/provision decisions, durable reservations, release draining, and structured provision plans. p229 adds task-graph scheduler integration by making scheduled nodes reserve agents, persist reservation metadata, perform queued ticket drain for graph work, and perform result-time release when graph nodes complete or fail. p230 adds workflow scheduler allocation: DOT `codergen` and `fan_out` request role/capability allocations, persist selected agent ownership, expose scheduler metadata in run events, and add release behavior by releasing reservations when workflow work completes. p231 adds existing-pane prompt reuse: routed online agents carry registry tmux targets into workflow allocation metadata, the tmux backend uses tmux rebind plus paste-buffer injection, and no duplicate spawn occurs for routed workflow dispatch. p232 adds queued codergen workflow wakeup, release-drain dispatch to the freed online Codex agent, and duplicate-dispatch suppression for replayed completion. p233 closes the remaining queued workflow wakeups / fan_out queued wakeups gap with queued fan_out reviewer wakeup, release-drain dispatch to the freed Codex review agent, and duplicate-dispatch suppression for replayed completion. This remains partial until dashboard views, Matrix/remote relay coverage, cutover, rollback, notification gates, and token provisioning are complete. | Phase E |
| runtime_launch_tmux | runtime_launch | required | /Users/zhangalex/Work/Projects/consult/agent-chat/bin/agent-up-v1 | partial | agentd has tmux backend pieces and real-run harnesses, and p231 adds workflow existing-pane prompt reuse for routed online agents by using tmux rebind and no duplicate spawn instead of launching a second session. p232 adds queued codergen workflow wakeup through release-drain dispatch, preserving duplicate-dispatch suppression when a queued prompt is delivered to the freed online runtime. p233 closes the remaining queued workflow wakeups / fan_out queued wakeups gap with queued fan_out reviewer wakeup through the same allocation-aware release-drain dispatch path. p234 adds operator `down`, explicit `rebind`, session recovery through stored tmux targets, and runtime lifecycle metadata without real Claude/tmux execution in tests. This remains partial until dashboard visibility, Matrix, remote relay coverage, cutover, rollback, notification gates, token provisioning, and full agent home/profile management are complete. | Phase B |
| dashboard_cli_operations | dashboard_cli | required | /Users/zhangalex/Work/Projects/consult/agent-chat/server.js | partial | Expand beyond run dashboard and `flow`/`run` commands to cover agents, queue, messages, blockers, service status, and parity audit. | Phase F |
| matrix_bridge | matrix_remote | required | /Users/zhangalex/Work/Projects/consult/agent-chat/bridge-matrix.js | partial | p236 adds the backend-facing external bridge contract: durable Matrix room trust and room mapping state, `POST /api/matrix/rooms`, `GET /api/matrix/rooms/:room_id`, Matrix inbound ingress through `POST /api/matrix/inbound`, event idempotency, `[AGENTIGNORE]` suppression, and Matrix outbox polling through `GET /api/matrix/outbox` that filters Matrix echo messages. p237 adds the `agentd-matrix` bridge runtime scaffold: backend/Matrix transport traits, deterministic `run_once` room registration, inbound forwarding, outbox polling, outbound send, and cursor advancement/retry behavior with fake backend and fake Matrix transport tests. p238 adds `AgentdHttpBackend` over standard-library HTTP for the p236 room/inbound/outbox contract and JSON cursor state persistence for restart-safe confirmed outbox progress. p239 adds `matrix-bridge-once`, a file-backed Matrix transport, one-shot bridge runner composition, and target-to-room resolution for trusted room mappings so the HTTP backend/runtime/state path can be replayed without a real homeserver. p240 adds the SDK-facing `MatrixClientPort` and `MatrixClientBridgeTransport` boundary, including login-before-sync, trust-mode invite handling, inbound loop suppression, and outbound text sends through the existing target-to-room directory. p241 adds the opt-in `matrix-sdk-adapter` feature and `SdkMatrixClient`, proving the real Matrix SDK dependency path compiles and exposing password login, access-token session restore, SDK sync, join, leave, and plain-text send operations through `MatrixClientPort` without enabling the SDK in default builds. p242 adds SDK timeline text parsing for original `m.room.message` sync timeline events into `MatrixClientTextMessage`, preserving event id, room id, sender MXID, body, Matrix mention user ids, and direct reply targets while skipping state, redacted, non-room-message, and malformed events. p243 adds puppet identity mapping with `MatrixPuppetDirectory` and `MatrixPuppetAccount`, generating known non-skipped local agent MXIDs, resolving only configured local puppet MXIDs, and keeping unknown `@ac_*` senders visible instead of treating every prefix match as an agent loop. p244 adds a local puppet account provisioning plan with `MatrixPuppetProvisioningPlan`, `MatrixPuppetTokenState`, `MatrixPuppetProvisioningConfig`, and `MatrixPuppetRegistrationAuth`, deriving agent-chat-compatible password candidates, preserving existing token names case-insensitively, reporting stale token names, and selecting registration-token or dummy UIA auth without making real Matrix requests. p245 adds the puppet account executor boundary with `MatrixPuppetAccountExecutor`, `MatrixPuppetAccountPort`, and `MatrixPuppetTokenSink`, validating existing tokens with whoami, trying login candidates, registering with the preferred password after login failures, saving new tokens, pruning stale token names, and reporting per-agent outcomes through fake-tested ports. p246 adds `MatrixPuppetHttpAccountConfig` and `MatrixPuppetHttpAccountPort`, a standard-library HTTP implementation of the p245 account port that sends Matrix Client-Server whoami, password login, registration probe, and registration-token or dummy UIA completion requests against fake homeservers in tests. p247 adds `MatrixPuppetHttpAccountProvisioner`, assembling `MatrixPuppetHttpAccountConfig`/`MatrixPuppetHttpAccountPort` with `MatrixPuppetAccountExecutor` so reusable token validation, login/register fallback, registration-token UIA, token saves, stale-token pruning, and per-agent failure continuation run together against fake homeservers and fake token sinks. p248 adds `MatrixPuppetTokenFileStore`, a file-backed `MatrixPuppetTokenSink` for agent-chat-style bridge-state JSON `agentTokens` that loads `MatrixPuppetTokenState`, preserves unknown bridge-state fields, replaces existing token keys case-insensitively, deletes stale token names, and writes through a temp-file-then-rename path. p249 wires p247 and p248 into the `matrix-bridge-once` assembly through optional `BridgeOncePuppetAccountConfig`: when configured, `run_bridge_once` runs `MatrixPuppetHttpAccountProvisioner` with `MatrixPuppetTokenFileStore` before the normal one-shot backend/file-transport pass, retains the puppet provisioning report, and `agentd matrix-bridge-once` accepts explicit puppet homeserver/server/agent/token-state/password/registration CLI options with incomplete configs rejected before HTTP side effects. p250 adds SDK-facing one-shot bridge assembly with `MatrixClientBridgeOnceConfig` and `run_matrix_client_bridge_once`, composing `AgentdHttpBackend`, `MatrixClientBridgeTransport`, `BridgeRuntime`, JSON cursor persistence, and optional puppet provisioning around any `MatrixClientPort` so fake Matrix clients can verify the path without enabling the real SDK by default. p251 adds daemon-side bounded Matrix client bridge service assembly with `MatrixClientBridgeServiceConfig`, `MatrixClientBridgeServiceReport`, `run_matrix_client_bridge_service`, and `agentd matrix-client-bridge-service`, reusing one mutable `MatrixClientPort` across iterations, preserving cursor progress only after successful iterations, and keeping the real `SdkMatrixClient` service entrypoint behind the `matrix-sdk-adapter` feature while default tests use fake clients only. p252 adds a Matrix client bridge operator preflight with `MatrixClientBridgePreflightReport`, `MatrixHomeserverPreflightReport`, `run_matrix_client_bridge_preflight`, and `agentd matrix-client-bridge-preflight`, reusing the p251 service option/config validation surface, probing `/_matrix/client/versions`, optionally probing access-token whoami, and avoiding bridge cursor or puppet token-state mutation while tests use fake homeservers only. p253 adds the explicit `scripts/agentd_matrix_client_bridge_preflight_smoke.sh` real-environment smoke harness gated by `AGENTD_REAL_MATRIX_PREFLIGHT_SMOKE`, capturing `preflight.out`, `preflight.err`, and `summary.txt`, redacting access tokens in plan/summary output, and verifying that `matrix-client-bridge-preflight` does not create the bridge cursor state file while tests use fake binaries only. p254 adds the explicit `scripts/agentd_matrix_client_bridge_service_smoke.sh` bounded service smoke harness gated by `AGENTD_REAL_MATRIX_SERVICE_SMOKE`, building `agentd-bin` with `--features matrix-sdk-adapter` unless `--skip-build` is passed, running `matrix-client-bridge-preflight` before `matrix-client-bridge-service`, capturing `preflight.out`, `preflight.err`, `service.out`, `service.err`, and `summary.txt`, redacting Matrix password/access-token/puppet-secret/registration-token values, and verifying that the bounded service command creates the bridge cursor state file while tests use fake binaries only. p255 adds the `agentd-matrix` bot command planner for agent-chat-compatible bang command parsing, Matrix mention-prefix stripping, command tier classification, ACL authorization, room context preservation, and non-command fallback replies without executing Matrix sends, backend mutation, tmux control, or agent runtimes. p256 adds bot command ingress classification in the SDK-facing Matrix client transport: `MatrixClientTextMessage` preserves optional `formatted_body`, `MatrixClientBridgeTransport` applies loop/ignored-sender suppression before planning commands, stores command-shaped events in `bot_command_plans`, omits commands from normal `inbound_events`, preserves normal non-command forwarding, and `agentd matrix-client-bridge-service` / `matrix-client-bridge-preflight` accept `--matrix-operator` and `--matrix-admin` ACL MXIDs. p257 adds read-only bot command replies for `!help`, `!status`, `!agents`, `!agents all`, `!groups`, ACL denial, unknown commands, and unsupported-command notices; it reads `/api/agents` and `/api/groups` through `AgentdHttpBackend`, sends replies through the SDK client transport, keeps command events out of inbound forwarding, and reports `bot_command_replies_sent` in one-shot/service runs. p258 adds management command effects for `!dm` and `!identity`, introducing backend and room effect ports, agent lookup, human-agent DM room requests, identity update requests, side-effect declarations, SDK transport execution through fake-tested ports, and HTTP backend request shapes for `GET /api/agents/:name` plus `PATCH /api/agents/:name`. p259 adds daemon identity persistence for `PATCH /api/agents/:name`, storing trimmed identity text in `runtime_profile.identity`, preserving existing runtime profile keys, rejecting empty identity text, and persisting the update through production daemon router rebuilds. p260 adds SDK-facing DM room lifecycle for `!dm`, using the sender full Matrix MXID, deriving the agent puppet MXID through `MatrixPuppetDirectory`, reusing trusted direct rooms, creating missing direct rooms named `DM: <agent>` with human and agent puppet invitees, reporting invite failures with room links, and keeping the real `SdkMatrixClient` lifecycle operations behind the existing `matrix-sdk-adapter` feature. p261 adds group management effects for `!mkgroup`, `!addmember`, `!rmember`, and `!rmgroup`, routing authorized Matrix bot commands through backend effects and daemon group APIs including `DELETE /api/groups/:name`, while leaving `!joingroup`, admin commands, and Matrix room cleanup outside this slice. p262 adds `!joingroup` backend membership plus trusted group-room invite effects, storing the sender human localpart in the daemon group and inviting the sender full Matrix MXID when a trusted group-room mapping exists. This remains partial until daemon/SDK account provisioning assembly evidence, daemon service assembly evidence, full daemon/SDK service assembly evidence, real homeserver validation in CI or operator evidence, real homeserver and daemon service evidence in CI or operator runs, real account registration against an operator homeserver, real Matrix HTTP account registration, additional durable token-store backends beyond the file bridge-state store, broader full timeline parsing, real SDK DM room lifecycle, real homeserver DM lifecycle evidence, per-agent puppet client execution evidence, room lifecycle parity, Matrix media transfer, admin commands, Matrix room cleanup, remaining management commands and full management command execution beyond p262, service packaging, service cutover, rollback, token provisioning, token rotation, bridge operations, and dashboard/operator visibility are complete. | Phase G |
| remote_relay | matrix_remote | required | /Users/zhangalex/Work/Projects/consult/agent-chat/remote/lib/push-relay-core.js | partial | p235 adds the first backend-facing remote relay compatibility baseline: durable server heartbeat state, `/api/servers/heartbeat`, durable delivery-event audit through `/api/delivery-events` and `/api/agents/:name/delivery-events`, and an agent-chat-compatible `/api/stream` message wakeup stream. This remains partial until a real remote package, install verification, relay process operation, tmux injection, Matrix bridge integration, service cutover, rollback, token provisioning/rotation, and dashboard relay views are complete. | Phase G |
| migration_shadow_cutover | migration_cutover | required | /Users/zhangalex/Work/Projects/consult/agent-chat/backend-v2.js | partial | p216 adds non-destructive `agents.json` import and agent shadow audit for the existing registry schema; p224 adds non-destructive message import, message shadow audit, group import, and cursor preservation for `messages.json`, `groups.json`, and `cursors.json`; p225 adds non-destructive task import, task-graph snapshot preservation, and shadow audit for `tasks.json` and optional `task_graphs.json`, covering imported tasks and task graphs at the compatibility-snapshot layer; p226 adds live task CRUD by making imported task rows editable as live agent-chat-compatible `/api/tasks` CRUD rows after cutover; p227 adds live `/api/task-graphs` DAG dispatch and result handling on the same compatibility table so imported task-graph state can continue after cutover; p228 adds durable scheduler reservations, queue, provision plans, `/api/pool`, `/api/dispatch`, and release state needed for later scheduler cutover; p229 adds task-graph scheduler integration for scheduled nodes, reservation metadata, queued ticket drain, and result-time release on live graph rows; p230 adds workflow scheduler allocation for `codergen`/`fan_out`, scheduler metadata in run events, and release behavior; p231 adds existing-pane prompt reuse for routed online agents via tmux rebind and no duplicate spawn; p232 adds queued codergen workflow wakeup, release-drain dispatch, and duplicate-dispatch suppression for replayed completion; p233 closes the remaining queued workflow wakeups / fan_out queued wakeups gap with queued fan_out reviewer wakeup, release-drain dispatch, and duplicate-dispatch suppression for replayed reviewer completion; p234 adds `down`, `rebind`, session recovery, and runtime lifecycle metadata needed for lifecycle-aware cutover. This remains partial until dashboard message/task views, Matrix, remote relay state, notification gates, service cutover, rollback automation, token provisioning, and full agent home/profile management are covered. | Phase H |
| api_auth_boundary | auth | required | /Users/zhangalex/Work/Projects/consult/agent-chat/backend-v2.js | partial | p215 adds a configured bearer guard for operator routes, agent token checks through `X-Agent-Token` with hard/audit modes, and local-only protection for launch-env/start. This remains partial until dashboard browser auth, bridge secrets, remote relay credentials, import-time token provisioning, and token rotation are covered. | Phase C |
| workload_identity_mtls | enterprise_identity | required | /Users/zhangalex/Work/Projects/consult/agent-chat/README.md | partial | Enterprise replacement gap (no agent-chat equivalent): the AD-E1 candidate verifies client certificate chains and validity with rustls, extracts one SPIFFE-compatible URI SAN, binds certificate fingerprints to the current P267 worker incarnation, and rejects untrusted, expired, revoked, or stale identities. Production trust-root distribution, control-plane/gateway enrollment, rotation operations, and deployment acceptance remain pending. | AD-E1 |
| tenant_project_authorization | enterprise_policy | required | /Users/zhangalex/Work/Projects/consult/agent-chat/README.md | partial | Enterprise replacement gap (no agent-chat equivalent): the AD-E1 code candidate adds immutable Specify-owned organization/project/snapshot scope, an independent tenant authorization port, cross-tenant rejection, repository-backed enterprise-principal lifecycle, pinned-key OIDC verification, Matrix user/device/appservice resolution, and ordered enterprise composition. Production Specify authorization transport, API/storage-wide rollout, SCIM/provisioning operations, and manual acceptance remain pending. | AD-E1 |
| fenced_attempt_capabilities | enterprise_admission | required | /Users/zhangalex/Work/Projects/consult/agent-chat/README.md | partial | Enterprise replacement gap (no agent-chat equivalent): the AD-E1 candidate issues OS-random opaque capabilities, persists only SHA-256 digests, binds validation to exact action/resource/snapshot, current P270 lease and fencing token, current worker incarnation, policy epoch, expiry, and revocation, and audits rejection before denial. Forge, object-store, delivery, and worker-protocol integration remain pending. | AD-E1 |
| scoped_secret_broker | enterprise_secret | required | /Users/zhangalex/Work/Projects/consult/agent-chat/README.md | partial | Enterprise replacement gap (no agent-chat equivalent): the AD-E1 code candidate adds zeroize-on-drop values, exact `secret.checkout` admission, nondisclosing denial, redacted audit, and `RemoteSecretBroker` over an authenticated scope/expiry-validating transport. Production Vault/KMS/cloud transport implementations, transient OCI injection, rotation drills, and operational acceptance remain pending. | AD-E1 |
| execution_sandbox_isolation | enterprise_sandbox | required | /Users/zhangalex/Work/Projects/consult/agent-chat/README.md | partial | Enterprise replacement gap (no agent-chat equivalent): the AD-E1 candidate adds an OCI sandbox profile and adapter with immutable image digests, read-only root, ephemeral workspace, dropped capabilities, no-new-privileges, seccomp, resource limits, tenant-only cache, default-deny egress, typed argv, idempotent cleanup, and an opt-in Docker/Podman smoke. Production image/profile rollout, executed real-container evidence, escape testing, and fleet integration remain pending. | AD-E1 |
| enterprise_principal_oidc_matrix | enterprise_identity | required | /Users/zhangalex/Work/Projects/consult/agent-chat/bridge-matrix.js | partial | The AD-E1 code candidate persists non-secret enterprise-principal, OIDC subject, Matrix user/device/appservice lifecycle state; verifies pinned RS256 issuer/audience/kid/expiry claims; and fails closed for disabled principals, revoked devices, foreign homeservers, and untrusted appservices. Real IdP/homeserver provisioning, rotation, deprovisioning drills, and authenticated gateway wiring remain pending. | AD-E1 |
| content_redaction | enterprise_security | required | /Users/zhangalex/Work/Projects/consult/agent-chat/README.md | partial | The AD-E1 code candidate compiles bounded exact-byte and policy-regex rules, rejects zero-length matches, applies deterministic leftmost-longest replacement to binary/UTF-8 content, and wires redaction to production sandbox stdout/stderr before delivery. Transcript persistence and real injected-secret evidence remain pending. | AD-E1 |
| placement_revocation | enterprise_security | required | /Users/zhangalex/Work/Projects/consult/agent-chat/README.md | partial | The AD-E1/AD-E2 candidates check data classification, region, trust domain, canonical signed image digest, dedicated pool, egress, cache isolation, and current Specify epoch at dispatch/renewal/artifact/delivery/release checkpoints. AD-E2 wires trusted worker inventory into pull placement and requires fenced epoch-aware artifact acknowledgements and side-effect admissions. Production authority transport and real stale-side-effect drills remain pending. | AD-E1/AD-E2 |
| native_runtime_process | native_runtime | required | /Users/zhangalex/Work/Projects/consult/agent-chat/bin/agent-up-v1 | missing | `AgentdWorker` owns the future live PTY/process runtime; current tmux launch remains compatibility evidence only. Planned P276. | AD-E5 |
| native_runtime_session_restore | native_runtime | required | /Users/zhangalex/Work/Projects/consult/agent-chat/bin/agent-up-v1 | missing | `AgentdControlPlane` must persist provider-native logical sessions while `AgentdWorker` restores the live process without using tmux pane identity. Planned P277. | AD-E5 |
| durable_runtime_identity | enterprise_runtime | required | /Users/zhangalex/Work/Projects/consult/agent-chat/backend-v2.js | partial | P265 defines the identity contract in `2026-07-10-enterprise-runtime-worker-identity-contract.md`; P267 adds typed profile/worker/incarnation/session/attempt ids plus additive storage in `0013_enterprise_agent_worker_runtime.sql`; P270 adds canonical `LeaseId`, `FencingToken`, and durable lease/head records in `0015_enterprise_task_leases.sql`. Full implementation remains pending beyond these stores and APIs: worker protocol/runtime binding, compatibility cutover, and rollback remain pending. | AD-E2/AD-E5 |
| project_room_repo_binding | enterprise_authority | required | /Users/zhangalex/Work/Projects/consult/agent-chat/bridge-matrix.js | partial | P266 defines immutable project, repository/base commit, room binding, frozen spec, workflow, policy, quota, and execution snapshot references in `2026-07-10-enterprise-project-room-repo-reference-contract.md`. P269 adds `ProjectAuthorityPort`, explicit `LocalProjectAuthority`, fail-closed transport-injected `SpecifyProjectAuthority`, and control-plane pin/recovery decisions. Specify network and durable pinning integration remain pending, together with legacy mapping, Matrix command normalization, authority-backed RBAC/quota enforcement, and cutover. `MatrixRobrixTransport` transports room activity but does not own the binding. | AD-E0/AD-E3 |
| native_matrix_gateway | matrix_remote | required | /Users/zhangalex/Work/Projects/consult/agent-chat/bridge-matrix.js | partial | The AD-E3 code candidate adds authenticated transport provenance, live enterprise-principal resolution, Specify-owned room/snapshot/command ACL binding, normalized execute/status/cancel commands, content-addressed attachments, immutable command/inbox ledgers, atomic optional-run/outbox/cursor handoff, canonical replay, bounded typed summaries, durable delivery acknowledgement, and startup-closed provider composition. Real homeserver and Robrix service assembly, restart/replay evidence, and operator cutover remain pending. | AD-E3 |
| matrix_project_cutover | migration_cutover | required | /Users/zhangalex/Work/Projects/consult/agent-chat/backend-v2.js | partial | The AD-E3 code candidate owns per-project observe, shadow-read-only, canary, active, drain, retire, and rollback modes with immutable transition history and cursor preconditions. Observe/shadow do not create runs or outbox effects; rollback disables execution; Robrix reads bounded project/run/task/artifact/approval/evidence projections without raw prompts, answers, logs, findings, or transcripts. Real dual-read comparison, in-flight rollback drill, legacy state mapping, service retirement, and operator sign-off remain pending. | AD-E3 |
| worker_fleet_protocol | enterprise_worker | required | /Users/zhangalex/Work/Projects/consult/agent-chat/remote/lib/push-relay-core.js | partial | The AD-E2 code candidate adds mTLS-authenticated heartbeat/pull/renew/result/artifact/side-effect services, monotonic availability, protocol/capacity/zone/drain state, stale-incarnation rejection, reaping, and structured explain output. Real listener deployment, multi-host loss/recovery, partition drills, and compatibility cutover remain pending. | AD-E2 |
| durable_task_leases | enterprise_scheduler | required | /Users/zhangalex/Work/Projects/consult/agent-chat/backend-v2.js | partial | `AgentdControlPlane` owns canonical state. The AD-E2 candidate composes the P270 lease head with migration `0018` queue, receipt, upload acknowledgement, side-effect, rejection, and outbox records; pull allocates queue/lease/fencing/outbox atomically and fail/reap paths implement bounded retry/dead-letter without token reuse. Scheduler tickets and reservation ids remain noncanonical compatibility input. Restart, partial-upload, network-partition, dual-write, cutover, and rollback acceptance remain pending. | AD-E2 |
| auth_rbac_quota | enterprise_policy | required | /Users/zhangalex/Work/Projects/consult/agent-chat/backend-v2.js | partial | `SpecifyProjectAuthority` owns versioned RBAC/quota policy intent; `AgentdControlPlane` owns enforcement decisions and measured usage. P215 provides the local bearer/agent-token boundary; P271 adds `UsageLedgerPort` with typed, idempotent measured-usage audit events, bounded replay, and totals. Policy authorization, budget reservation, secret rotation, and P279 enforcement/doctor diagnostics remain pending. | AD-E1 |
| artifact_audit_provenance | enterprise_audit | required | /Users/zhangalex/Work/Projects/consult/agent-chat/lib/mcp-server-core.js | partial | P268/P271 add immutable artifact/audit/usage contracts and fenced rejection audit. AD-E2 adds fenced upload acknowledgements. The AD-E4 candidate adds canonical signed Builder/Worker evidence, immutable request/result event ledgers, OpenFab Ed25519 verification, current-policy/exact-subject Forge admission, certification state history, and authenticated bounded HTTPS transport while preserving `OpenFabCertificationAuthority` ownership of verdicts and signatures. Deployed OpenFab interoperability, object storage, outage/replay drills, cutover, and operator acceptance remain pending. | AD-E2/AD-E4 |
| openfab_certification_protocol | enterprise_certification | required | no agent-chat equivalent | partial | The AD-E4 candidate binds immutable snapshot/source/spec/evidence/policy/skill digests, stores request and externally signed result as separate exact-idempotent events, verifies registered OpenFab `did:key` signatures, enforces gate=none/machine/human N-of-M Forge semantics, and fails startup closed without signer/verifier/store/state/transport/revocation/clock providers. Real OpenFab service deployment, independent verification, failure injection, and FSF-5 sign-off remain pending. | AD-E4 |
| skill_hub_package_trust | enterprise_supply_chain | required | no agent-chat equivalent | partial | The AD-E4 candidate pins exact package/version/archive/manifest/dependency-lock/permissions hashes in Specify snapshots and evidence, resolves signed time-bounded OpenFab trust over HTTPS, admits only approved/signed package versions, and preserves immutable trust-at-install history after yank/revoke. Real Skill Hub publication, package installation, status transition drills, and operator acceptance remain pending. | AD-E4 |
| operational_doctor_health | enterprise_operations | required | /Users/zhangalex/Work/Projects/consult/agent-chat/server.js | missing | `AgentdControlPlane` must expose project-authority, worker, lease, queue, runtime, artifact, Matrix, and recovery diagnostics without requiring raw-log inspection. Planned P279. | AD-E2/AD-E7 |
