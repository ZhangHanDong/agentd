# Agentd real execute smoke artifact

This repository artifact confirms that the real `execute.dot` path can apply a
frozen specification in an allocated implementation worktree and make the
verified result available for publication.

`AGENTD_REAL_EXECUTE_SMOKE_READY`
