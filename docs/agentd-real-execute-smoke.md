# Agentd Real Execute Smoke

This artifact verifies that the real `execute.dot` path can assign an
implementer to an allocated worktree and produce a publishable change.

Marker: `AGENTD_REAL_EXECUTE_SMOKE_READY`
