# agentd Real Execute Smoke Artifact

This document is the repository artifact produced by the real `execute.dot`
implementation path. Its integration test reads this file at compile time to
verify that the allocated worktree contains the published change.

`AGENTD_REAL_EXECUTE_SMOKE_READY`
