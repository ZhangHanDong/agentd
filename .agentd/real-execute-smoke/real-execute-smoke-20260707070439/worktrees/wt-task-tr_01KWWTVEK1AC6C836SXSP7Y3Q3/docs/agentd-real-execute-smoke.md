# agentd real execute smoke

AGENTD_REAL_EXECUTE_SMOKE_READY

This file is intentionally small. It exists only to prove that an execute.dot
run can carry a frozen spec through implementation, verification, review,
publication, and PR creation.
